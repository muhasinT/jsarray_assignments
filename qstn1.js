const a = "w3resource";

const b = [1,2,4,0];

let is_array  = Array.isArray(a);

console.log(is_array + ` So "w3resource" is not an array`);

let is_array1  = Array.isArray(b);

console.log(is_array1 + ` So [1,2,4,0] is an array`);